name: Deploy apps to GitHub Pages

on:
  push:
    branches: ["master"]
    paths:
      - "travelnow/**"
      - "rawfotra/**"
      - "tipclip/public/**"
      - ".github/workflows/pages.yml"
  workflow_dispatch:

# Allow this workflow to publish a GitHub Pages site
permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment; don't cancel an in-progress one
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5
        with:
          # Enable Pages (with GitHub Actions as the source) if not already on
          enablement: true

      - name: Build RAWFOTRA (minified)
        # The Pages site serves only the mangled build output, never the source.
        run: |
          cd rawfotra
          npm install --no-audit --no-fund
          npm run build

      - name: Assemble site
        # TravelNow keeps the site root (existing URLs unchanged);
        # RAWFOTRA v6.4 (built) is served under /rawfotra/, TipClip's web prototype under /tipclip/
        run: |
          mkdir -p _site
          cp -r travelnow/. _site/
          cp -r rawfotra/dist _site/rawfotra
          cp -r tipclip/public _site/tipclip
          touch _site/.nojekyll

      - name: Upload site artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./_site

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

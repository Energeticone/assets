__pycache__/
*.pyc
.env

# Runtime state is the firm's compounding asset — keep the directory, ignore the
# generated contents so demos start clean. Remove these lines if you want to commit
# and version your firm's learning loop (recommended for a real deployment).
state/knowledge.json
state/veteran.json
state/traces.jsonl
